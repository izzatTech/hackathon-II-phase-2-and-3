/** @type {import('next').NextConfig} */
const nextConfig = {
  // Enable using src directory for pages
  // Note: Next.js 13+ recommends the app directory structure
  // But we're using the pages router in the src directory
};

module.exports = nextConfig;